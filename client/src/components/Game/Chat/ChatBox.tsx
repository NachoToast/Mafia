import { Box } from '@mui/material';
import React from 'react';

const ChatBox = () => {
    return (
        <Box style={{ overflowY: 'auto', height: '100vh', backgroundColor: 'gray' }}>
            chat box hmm yes
        </Box>
    );
};

export default ChatBox;
