import { Box } from '@mui/material';
import React from 'react';

const RoleList = () => {
    return <Box style={{ backgroundColor: 'pink', height: '33%' }}>role list</Box>;
};

export default RoleList;
