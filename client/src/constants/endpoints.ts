/** Endpoint for the websocket to hit. */
export const serverEndpoint: string = 'http://ntgc.ddns.net';

/** Port for the websocket to use. */
export const serverPort: number = 3001;

/** Name of the server hub. */
export const serverName: string = 'mafia';
